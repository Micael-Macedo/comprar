import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        width: "100%",
        flexDirection: 'row',
        alignItems: "center",
        gap: 5
    },
    title: {
        fontSize: 12,
        fontWeight: 600
    }
})